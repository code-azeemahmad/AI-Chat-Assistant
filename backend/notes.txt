Depend on abstractions, not implementations.

                Router
                   │
                   ▼
             ChatService
                   │
                   ▼
          LLMProvider (ABC)
                   │
     ┌─────────────┼─────────────┐
     ▼             ▼             ▼
OllamaProvider OpenAIProvider GeminiProvider


Notice that ChatService depends only on the interface.

_______________________________________________________

Conceptually, every provider should answer this question:

"Given a list of chat messages and generation parameters, produce a response."

It doesn't matter whether that provider talks to:

Ollama
OpenAI
Gemini
Claude

The service interacts with them the same way.

_______________________________________________________

Responsibilities of OllamaProvider

Only four responsibilities:

Convert ChatRequest → Ollama request
Call Ollama
Convert Ollama response → ChatResponse
Raise meaningful exceptions

It must not:

Save messages
Build prompts
Perform authentication
Know about FastAPI
Know about routers

_______________________________________________________

Improved Architecture
                 Router
                    │
                    ▼
              ChatService
                    │
                    ▼
            ProviderFactory
                    │
          ┌─────────┼─────────┐
          ▼         ▼         ▼
     Ollama   OpenAI   Gemini
          │
          ▼
     LLMProvider

Notice:

The service never knows which provider exists.
The factory chooses.

_______________________________________________________

Dependency Injection Flow

FastAPI builds the dependency graph automatically.

Request

↓

Router

↓

Depends(get_chat_service)

↓

Depends(get_llm_provider)

↓

Depends(get_http_client)

↓

AsyncClient

_______________________________________________________

New Dependency Graph
Request
      │
      ▼
Router
      │
      ▼
Depends(get_chat_service)
      │
      ▼
Depends(get_llm_provider)
      │
      ▼
Depends(get_http_client)
      │
      ▼
request.app.state.http_client

No object is created per request.

After These Two Changes

Your architecture becomes:

HTTP

↓

Schemas

↓

Domain

↓

Service

↓

Provider

↓

Provider Models

↓

HTTP Client

↓

Ollama

This is a very clean separation of concerns.

_______________________________________________________

__Introduce mapper__

request_schema
        │
        ▼
ChatMapper.to_domain()
        │
        ▼
service.chat()
        │
        ▼
ChatMapper.to_schema()

Establish the Schema ↔ Domain conversion layer

_______________________________________________________

                    FastAPI Application
                           │
          ┌────────────────┴────────────────┐
          │                                 │
          ▼                                 ▼
   Exception Handlers                  Lifespan
          │                                 │
          ▼                                 ▼
      API Routers                    Shared AsyncClient
          │
          ▼
     Chat Router
          │
          ▼
     Chat Mapper
          │
          ▼
     ChatService
          │
          ▼
     LLMProvider
          │
          ▼
    OllamaProvider
          │
          ▼
     Ollama Server

_______________________________________________________